import React from "react";

import {StyledSubTitle} from "../../styles/styles"

const SubTitle = ({children}) => {

    return (
        <StyledSubTitle>
            {children}
        </StyledSubTitle>
    )
}

export default SubTitle;